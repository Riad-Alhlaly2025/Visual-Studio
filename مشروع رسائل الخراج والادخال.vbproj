Public Class Form1
    Private Sub Button2_Click(sender As Object, e As EventArgs) Handles Button2.Click
        End
    End Sub

    Private Sub Button3_Click(sender As Object, e As EventArgs) Handles Button3.Click
        Dim i As String

        i = InputBox("15 الرجا ادخال عدد بين الصفر و", "صندوق الادخال ")

        If i >= 0 And i <= 15 Then
            Me.BackColor = System.Drawing.ColorTranslator.FromOle(QBColor(Val(i)))

        Else
            MsgBox("الادخال خاطئ", , "تنبية")
        End If
    End Sub

    Private Sub Button4_Click(sender As Object, e As EventArgs) Handles Button4.Click
        Randomize()
        Me.BackColor = System.Drawing.ColorTranslator.FromOle(RGB(Rnd() * 255, Rnd() * 255, Rnd() * 255))
    End Sub

    Private Sub Label1_Click(sender As Object, e As EventArgs) Handles Label1.Click

    End Sub

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        Dim w As String
        Dim x As String
        Dim y As String

        w = Chr(13) + Chr(10)

        x = "جامعة الملك سعود" + w + "مركزالتدريب لخدمة المجتمع " + w + " دبلوم الحاسب الالي" + w + "اعداد \ رياض الهلالي" + w + "العام الدراسي ( 2025)"

        y = "حول المشروع"

        MsgBox(x, MsgBoxStyle.Information + MsgBoxStyle.MsgBoxRight, y)

    End Sub

    Private Sub Button5_Click(sender As Object, e As EventArgs) Handles Button5.Click
        Dim w As String

        w = Chr(13) + Chr(10)

        TextBox1.Text = "الحمد لله" + w + "رب العالمين"
        Label1.Text = " الحمد لله" + w + "رب العالمين"
    End Sub

    Private Sub Button6_Click(sender As Object, e As EventArgs) Handles Button6.Click
        Dim w As String
        w = Chr(13) + Chr(10)
        MsgBox("اعداد " + w + "1\ رياض الهلالي" + w + "******* " + w + "تحت اشراف المهندس \ رياض صالح ")
    End Sub
End Class
